import { Data, Direction, NamePart } from '../../Data._ts';
import { Realm, Word } from '../../realm/Realm';
import Util from '../../Util';
import { Biome } from './BiomesModule._ts';

export type River = {
  name: Word;
  flowsTo: Direction;
  flowsToCoast: Boolean;
  flowsFrom: Direction;
  flowsFromMountains: Boolean;
  tributaries: River[];
  prefix: NamePart | null;
  stem: River | null;
};

export class RiverModule {
  static determine(realm: Realm) {
    return realm;
  }
}

// todo rebuild river module
